/**
 * loads and decorates the contact card
 * @param {Element} block The contact card block element
 */
export default function decorate(block) {
  block.querySelector('a').classList.add('secondary');
}
